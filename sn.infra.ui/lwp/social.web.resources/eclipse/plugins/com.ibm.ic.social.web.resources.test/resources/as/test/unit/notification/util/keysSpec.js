/* Copyright IBM Corp. 2015  All Rights Reserved.                    */

define([
	"ic-as/notification/util/keys"
], function(keys) {
		
		
		describe("the ic-as/notification/util/keys class", function() {
			it("implements the expected methods", function() {
				//expect(nc.toggle).toEqual(jasmine.any(Function));
			});
		});

});
